namespace Vendas.Domain.Clientes.Enums
{
    //É Indicador - tabela de indicador (está ENUM para fins didáticos)
    public enum Sexo
    {
        NaoInformado = 0,
        Masculino = 1,
        Feminino = 2,
        Outro = 3,
    }
}
